{
  "name": "express-app",
  "version": "0.1.0",
  "scripts": {
    "test": "mocha -r should --recursive",
    "test-watch": "mocha -r should --recursive -w -R min -G",
    "test-cov": "istanbul cover _mocha -- -r should --recursive"
  },
  "dependencies": {
    "consolidate": "^0.10.0",
    "express": "^4.3.2",
    "hogan.js": "^2.0.0"
  },
  "devDependencies": {
    "istanbul": "^0.2.10",
    "mocha": "^1.20.0",
    "should": "^4.0.0",
    "sinon": "^1.10.1",
    "sinon-lazy-stub": "^0.1.0"
  }
}
