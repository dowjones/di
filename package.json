{
  "name": "areus-ioc",
  "version": "0.1.0",
  "description": "Areus IOC Container",
  "main": "./lib/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha -r should --recursive",
    "test-watch": "mocha -r should --recursive -w -G -R min"
  },
  "license": "MIT",
  "devDependencies": {
    "mocha": "^1.19.0",
    "should": "^3.3.2",
    "sinon": "^1.10.0"
  },
  "dependencies": {
    "resolve": "^0.7.4"
  }
}
